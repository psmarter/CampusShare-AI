<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Campus_item_sharing"
        tools:targetApi="31"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- 主 Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Campus_item_sharing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 声明 LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Campus_item_sharing" />

        <!-- 声明 PersonalInfoActivity -->
        <activity
            android:name=".account.PersonalInfoActivity"
            android:exported="true"
            android:theme="@style/Theme.Campus_item_sharing" />

        <!-- 声明 PasswordEditActivity -->
        <activity
            android:name=".account.PasswordEditActivity"
            android:exported="true"
            android:theme="@style/Theme.Campus_item_sharing" />

        <!-- 声明 PostActivity -->
        <activity
            android:name=".post.PostActivity"
            android:exported="true"
            android:theme="@style/Theme.Campus_item_sharing" />

        <!-- 声明 PostInfoActivity -->
        <activity
            android:name=".post.PostInfoActivity"
            android:exported="true"
            android:theme="@style/Theme.Campus_item_sharing" />

        <!-- 声明 ChatActivity -->
        <activity
            android:name=".ChatActivity"
            android:exported="true"
            android:theme="@style/Theme.Campus_item_sharing" />

        <!-- 声明 GeminiActivity -->
        <activity
            android:name=".GeminiActivity"
            android:exported="true"
            android:theme="@style/Theme.Campus_item_sharing" />

        <!-- 声明 WebActivity -->
        <activity
            android:name=".function.WebActivity"
            android:exported="true"
            android:theme="@style/Theme.Campus_item_sharing" />

    </application>

</manifest>